use crate::organism::{Offspring, Organism, OrganismContext};

// let mut organism = Organism::new(seed_org);
    // let inputs: Vec<i64> = (0..3).map(|_| world.rng.random()).collect();
    // let mut octx = OrganismContext::new(inputs);
    // let mut icount = 0;
    // let mut offspring: Option<Offspring> = None;

    // while icount < 10000 && offspring.is_none() {
    //     offspring = organism.process_step(&mut world, &mut octx);
    //     icount += 1;
    // }

    // if let Some(offspring) = offspring {
    //     println!("offspring:     {}", offspring.genome.as_short_string());
    // } else {
    //     println!("offspring:     <none>");
    // }
